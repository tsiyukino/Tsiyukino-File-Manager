<Window x:Class="TSFM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TSFM"
        mc:Ignorable="d"
        Title="File Manager" Height="800" Width="1280"
        MinHeight="600" MinWidth="900">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#4285f4" Offset="0"/>
                <GradientStop Color="#1a73e8" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <ContentControl x:Name="SidebarContent"/>
        </Border>

        <!-- Main Content -->
        <Border Grid.Column="1" Background="#F0FFFFFF" CornerRadius="0">
            <ContentControl x:Name="MainContent"/>
        </Border>
    </Grid>
</Window>
